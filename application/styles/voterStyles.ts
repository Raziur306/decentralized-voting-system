import styled from "@emotion/styled";
import { Box } from '@mui/material'

export const StyledCounterBox = styled(Box)({
    display: 'flex',
    flexDirection: 'column',
    gap: 2,
    background: '#242B2E',
    padding: 20,
    borderRadius: 10
})